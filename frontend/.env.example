# Normal mode uses the real Trailsight V2 API.
# Leave blank during `npm run dev` to use Vite's /api proxy to http://127.0.0.1:8000.
VITE_TRAILSIGHT_API_BASE_URL=

# Fixtures are explicit only. Never enable this in a real runtime build.
# VITE_TRAILSIGHT_DATA_MODE=fixture
